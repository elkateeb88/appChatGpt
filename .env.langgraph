# MCP-LangGraph Bridge Configuration

# Port for MCP server (default: 8000)
PORT=8000

# NumeroAssistant-AI API URL (your local API)
LANGGRAPH_API_URL=http://localhost:8002/api/v1/chat

# User ID for MCP requests (used to track sessions)
MCP_USER_ID=mcp_chatgpt_user
